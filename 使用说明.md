# 山水清音 · 个人网站 使用说明

一个新中式风格的个人网站。**不需要任何编程知识**，跟着下面三步走，就能换成您自己的内容。

---

## 零、一键添加工具（最省事）

项目里有一个 **add-tool.html**，双击打开它（或通过本地预览打开）：

1. 选「添加照片 / 添加视频 / 添加游记」，填表；
2. 点「生成代码」→「复制代码」，粘贴到 data.js 对应位置；
3. 或者更省事：点「选择 data.js」导入文件 → 点「插入并下载」→ 用下载的 data.js 覆盖 js/data.js。

---

## 一、怎么打开网站

直接双击 `index.html` 即可在浏览器中打开。

> 小提示：如果想让手机也能访问，或部署到网上，把整个 `personal-website` 文件夹里的内容上传到任意静态托管（如 Gitee Pages、GitHub Pages、阿里云 OSS 等）即可。

---

## 二、怎么换成我的内容（重点）

所有文字内容都集中在一个文件里：

```
js/data.js
```

用记事本或任意编辑器打开它，**只修改引号 `" "` 里的文字**，保存后刷新网页即可生效。文件内每个位置都有中文注释标明用途。

| 我想改什么 | 去哪里改 |
|---|---|
| 名字、首页标题、诗句 | `data.js` 顶部 `site` 部分 |
| 个人简介、身份标签、数据统计 | `data.js` 的 `profile` 部分 |
| 旅行随笔（标题/正文/日期） | `data.js` 的 `travel` 部分 |
| 照片清单 | `data.js` 的 `photos` 部分 |
| 视频清单 | `data.js` 的 `videos` 部分 |
| 爱好卡片 | `data.js` 的 `hobbies` 部分 |
| 联系方式 | `data.js` 的 `contact` 部分 |

**增删条目**：每条内容是一个 `{ ... }` 段落。想删除就整段删掉；想新增就复制一段紧跟着粘贴，注意段落之间用逗号隔开。

---

## 三、怎么上传照片

1. 把您的照片复制进 `assets/images` 文件夹（建议 jpg / png / webp 格式）；
2. 打开 `js/data.js`，找到 `photos` 部分，按现有格式添加一行：

```js
{ src: "assets/images/我的照片1.jpg", title: "落日渡口", place: "厦门 · 沙坡尾" },
```

3. `src` 填照片文件名，`title` 是照片标题，`place` 是拍摄地点；
4. 保存并刷新网页。

**换头像/肖像**：把您的竖版照片（建议 3:4 比例）放进 `assets/images`，然后在 `data.js` 的 `profile.portrait` 一行把路径改为：

```js
portrait: "assets/images/我的肖像.jpg",
```

**给旅行随笔换配图**：同样把照片放进 `assets/images`，然后改 `travel` 里对应文章的 `cover` 路径。

> 目前网站里展示的是水墨风格的占位图，替换后自动消失。若某个图片路径写错了，网页也会自动显示占位图提醒您。

---

## 四、怎么上传视频

1. 把视频复制进 `assets/videos` 文件夹（建议 mp4 格式，单个文件建议小于 50MB，网页加载更流畅）；
2. 打开 `js/data.js`，找到 `videos` 部分，按格式登记：

```js
{
  src: "assets/videos/我的旅行混剪.mp4",   // 视频文件
  poster: "assets/images/视频封面.jpg",    // 封面图（可留空 ""，自动使用占位封面）
  title: "我的旅行混剪",                  // 标题
  sub: "漓江 · 敦煌 · 梅里"               // 副标题（拍摄地点等）
},
```

3. 保存刷新后，"影像集" 页签里就能直接在线播放。

---

## 五、常见问题

**Q：改了 data.js 但网页没变化？**
强制刷新一下：按 `Ctrl + F5`（Mac 按 `Cmd + Shift + R`）。

**Q：照片显示不出来？**
检查文件名是否完全一致（包括大小写和扩展名），路径应写成 `assets/images/文件名`。

**Q：想改颜色 / 字体？**
配色定义在 `css/style.css` 文件最顶部的 `:root` 区块（如朱砂红 `--cinnabar`、黛青 `--teal`、宣纸底 `--paper`），改色值即可。

**Q：随笔正文想换行分段？**
`content` 里每一段放在一对引号里，用逗号隔开，就是一段。

**Q：想加更多爱好图标？**
`hobbies` 的 `icon` 目前支持：brush（毛笔）、camera（相机）、tea（茶）、mountain（登山）、book（书）、guqin（琴）、chess（棋）、flower（花）、music（乐）、run（跑）。

---

## 文件结构一览

```
personal-website/
├── index.html            ← 网页入口（双击打开）
├── 使用说明.md            ← 本文件
├── css/
│   └── style.css         ← 样式（配色在顶部 :root）
├── js/
│   ├── data.js           ← ★ 您的内容都在这里
│   └── main.js           ← 渲染引擎（无需改动）
└── assets/
    ├── images/           ← 照片放这里
    │   └── *.svg         （水墨占位图，可保留作备用）
    └── videos/           ← 视频放这里
```
