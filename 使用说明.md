# 山水清音 · 个人网站 使用说明

一个新中式风格的个人网站。**不需要任何编程知识**，跟着下面三步走，就能换成您自己的内容。

---

## 零、一键添加工具（最省事）

项目里有一个 **add-tool.html**，双击打开它（或通过本地预览打开）：

1. 选「添加图集 / 添加平台入口 / 添加游记」，填表；
2. 点「生成代码」→「复制代码」，粘贴到 data.js 对应位置；
3. 或者更省事：点「选择 data.js」导入文件 → 点「插入并下载」→ 用下载的 data.js 覆盖 js/data.js。

- 添加图集：填城市名，照片清单每行一张（格式：`文件名 标题`，标题可省），**文件名不用打 .jpg，工具会自动补上**（png 等其它格式请写全），印章文字可留空；
  - **懒得手打清单就选文件夹**：点「选择图片文件夹…」或直接把文件夹拖到虚线框里，工具会把里面的图片按名称自然排序（`cq2` 会排在 `cq10` 前面）自动填进清单；
  - 三个勾选项：**追加到现有清单**（默认开，不覆盖已填内容）、**包含子文件夹**（默认关，只取这一层）、**用文件名当标题**（默认关，勾上则每行写成 `文件名 标题`）；
  - **图片所在文件夹**（默认 `assets/images`）：生成的链接前缀。选了「北京」这个文件夹后会自动变成 `assets/images/北京`，链接就写成 `assets/images/北京/xxx.jpg`；不对可手动改这一栏，改成 `assets/photos` 之类也行；
  - 勾了「包含子文件夹」时子目录结构会保留（`第一天/xxx.jpg` → `assets/images/北京/第一天/xxx.jpg`）；
  - 注意：选文件夹只负责填文件名和路径，**图片本身还要拷进对应目录**；
- 添加平台入口：填平台名、一句话说明、主页链接。

---

## 零点二、图集照片批量重命名（rename_album.bat）

照片多的时候，一张张改名太慢。**双击 rename_album.bat**，回答几个问题即可：

1. **英文前缀**：编号长在它后面，比如填 `weiman`，照片就叫 `weiman1`、`weiman2`……；
2. **照片文件夹**：直接回车 = `assets/images`；
3. **处理范围**：最近 N 张 / 某个时间之后导入的 / 全部 / 文件名包含某字；
4. **排序**：按导入时间（默认，等于你放进文件夹的顺序）或按文件名；
5. 起始编号、编号补零位数（如补 3 位得到 `weiman001`）；
6. 是否连**中文文件名**一起改（默认不改，见下方规则）。

**两条保护规则**（都可以关）：

- 文件名里**含中文的自动跳过**，保持原名不动，编号在剩下的图片上接着排（不跳号）。想连它们一起改：命令行加 `--allow-chinese`，问答模式在第 6 步填 `y`；
- 已经叫「前缀+数字」的文件自动跳过，重复运行不会乱编号。要连它们一起重编，加 `--force`。

被这两条挡下的文件**不会从清单里消失**：文件名保持原样，但照样写进清单，排在改名条目后面，整段复制即可。例如：

```
weiman1.jpg IMG_0102
weiman2.jpg IMG_0103
地质宫.jpg 地质宫      <- 中文名没改，照样在清单里
```

只要中文名文件也在这批范围内（同一个时间窗 / 同一批最近 N 张），就会被带上；不属于本批次的老文件不会混进来。确实只想列出改名部分，加 `--exclude-kept`。

> 因为中文名文件也算进「一批」里，`--newest N` 的 N 现在按**全部图片**计数（含中文名的），不是只数要改名的。

确认预览表后回车执行，会自动生成三个文件（默认放在项目根目录）：

| 文件 | 用途 |
|---|---|
| `<前缀>_照片清单.txt` | 整段粘进 add-tool.html 的「照片清单」框，改名与保持原名的都在里面 |
| `<前缀>_对照表.csv` | 编号 / 新文件名 / 标题 / 原文件名，Excel 双击不乱码；保持原名的行编号列写「原名保留」 |
| `<前缀>_datajs片段.txt` | 填了城市名才有，直接粘进 js/data.js 的 photos 数组 |

想改回原名，两种办法：

1. **双击 `rollback.bat`**，把 `<前缀>_对照表.csv` 直接拖到它上面（或运行后粘贴对照表路径）；
2. 命令行：

```
python rename_album.py --rollback "assets\images\cq2_对照表.csv"
```

路径要写对照表实际所在的位置（默认生成在项目根目录，若你指定了 `--out` 就去那个文件夹找）。脚本会先列出「将改成什么」并要你输入 `y` 才执行。标注「原名保留」的行会自动跳过，不会误动。

> 只改文件名，不压图不删图。已经叫「前缀+数字」的文件会自动跳过，重复运行不会乱编号。

---

## 零点五、一键压缩并统一转 JPG（上传前必做）

项目里有一个 **compress.bat**，双击它即可。**可以自己选要压缩哪个文件夹**：

1. 把新照片放进某个文件夹（png、webp、jpg 都行）；
2. 三种用法任选：
   - **双击 `compress.bat`**，输入文件夹路径后回车（**直接回车 = `assets/images`**）；
   - **把文件夹直接拖到 `compress.bat` 上**；
   - 命令行：`python compress.py --dir "F:\照片\敦煌"`（加 `-r` 连子文件夹一起处理）；
3. 脚本自动做两件事：
   - **统一转 jpg**：png、webp 等全部转成 `.jpg`（透明的地方补白底），网站里从此只有一种格式；
   - **压缩**：超过 800KB 或长边超过 1600px 的图，缩到长边 1600px；
4. 文件名变了（如 png→jpg）会自动同步 data.js 里的引用，不用手动改；
5. **不保留原图**：压缩后直接覆盖，图片在 `assets/images` 里只留一份（先写临时文件、成功才替换，中途出错原图不受影响）；
   唯一例外是 `a.png` 要转成的 `a.jpg` 已被另一张图占用时，会跳过并提示，不会覆盖也不会删。

> 已经是 jpg 且大小、尺寸都合格的照片会原样跳过，不会被反复压缩导致画质变差。
>
> 处理 `assets/images` 时才会顺带同步 data.js 的引用；压缩别的文件夹不会动 data.js（报告里会写明），可以放心拿它压任何地方的照片。

---

## 一、怎么打开网站

直接双击 `index.html` 即可在浏览器中打开。

> 小提示：如果想让手机也能访问，或部署到网上，把整个 `personal-website` 文件夹里的内容上传到任意静态托管（如 Gitee Pages、GitHub Pages、阿里云 OSS 等）即可。

---

## 二、怎么换成我的内容（重点）

所有文字内容都集中在一个文件里：

```
js/data.js
```

用记事本或任意编辑器打开它，**只修改引号 `" "` 里的文字**，保存后刷新网页即可生效。文件内每个位置都有中文注释标明用途。

| 我想改什么 | 去哪里改 |
|---|---|
| 名字、首页标题、诗句 | `data.js` 顶部 `site` 部分 |
| 个人简介、身份标签、数据统计 | `data.js` 的 `profile` 部分 |
| 旅行随笔（标题/正文/日期） | `data.js` 的 `travel` 部分 |
| 照片图集（按城市分组） | `data.js` 的 `photos` 部分 |
| 影像集（社交平台入口） | `data.js` 的 `social` 部分 |
| 点亮中国（去过的城市） | `data.js` 的 `cities` 部分（详见下面「点亮中国」一节） |
| 图集悬浮印章文字 | `data.js` 顶部 `site` 的 `photoSeal`，或某个城市里的 `seal` |
| 爱好卡片 | `data.js` 的 `hobbies` 部分 |
| 联系方式 | `data.js` 的 `contact` 部分 |

**增删条目**：每条内容是一个 `{ ... }` 段落。想删除就整段删掉；想新增就复制一段紧跟着粘贴，注意段落之间用逗号隔开。

---

## 二点五、点亮中国（去过的城市）

点击首页「其人」区里的 **「抵达城市」数字**，会弹出一张中国地图，点亮您去过的省份，下方列出具体城市名。这个环节完全独立于「行迹」里的照片和随笔，只认下面这份城市清单。

**点下面的城市名，地图上它所在的那个省就会亮起来**：省份变成浅一号的水红、描一圈墨黑边，并做 1.9 秒一轮的呼吸（微微缩放 + 淡黄色高光）；其余省份完全保持原色，不做任何淡化。城市与城市之间互不关联——点淄博只亮山东，「济南」这个词条不会被牵动，同省别的城市也一样。再点一次取消，点别的城市直接切换。鼠标划过可临时预览（未锁定时）。省份本身仍然不可点击。

**底部数据条还能点**：

四个数字都是可点的，**点下去的反馈完全一致**（轻按一下 + 目标轻微缩放落定），并且从上到下**依次渐入**（间隔 0.07 秒，位移 8px，幅度很小）：

- **行走年头** → 切到「行迹 · 旅行随笔」并平滑滚过去；
- **抵达城市** → 打开点亮中国地图；
- **快门次数** → 切到「行迹 · 照片集」并平滑滚过去；
- **爱好广泛度**（原「热爱程度」）→ 平滑滚到「所好」版块。

这几处动效都刻意做得很轻（数字按压 2.5% 幅度、版块缩放 1.6%、约 0.4 秒）。嫌太弱就在 `js/main.js` 的 `zoomPulse()` 里调 `scale(1.016)`，以及 `css/style.css` 的 `@keyframes statTap`；反之再往小调。

对应规则写在 `js/main.js` 的 `renderProfile()` 里，按标签名匹配：`抵达城市` / `年头` / `快门次数` / `热爱|爱好`。**第四条改成什么名字都行，只要含「热爱」或「爱好」两个字**，比如「爱好广泛度」「热爱程度」都能自动关联到「所好」版块。

**地图是三层画的**（`js/main.js` 的 `renderCityMap()` + `css/style.css`）：

| 层 | 元素 | 作用 |
|---|---|---|
| 填充层 | `.province-fill` | 只填色不描边，未去过米灰、去过朱砂 |
| 描边层 | `.province-line` | 只描边不填色，**深红 `#5A1D18`、宽 .6**，压在所有填充之上 |
| 高亮层 | `#map-active-layer` 里的 `.pa-fill` / `.pa-line` | 点城市时在最上层重画一份：填充 `#D9604B` + **深红描边 `#3A100B`（宽 1.3）**，整圈边界完整 |

之所以要拆层：SVG 按文档顺序绘制，若一个 path 同时带 fill 和 stroke，后画的邻省填充会盖掉先画省份靠外的一半描边，边界就会断断续续；高亮省若只在底层加粗描边，共享边界上同样会被邻省盖掉，所以要单独在最上层重画一份。

想调观感改这几个地方：

- 底图省界深浅粗细：`.province-line` 的 `stroke` 与 `stroke-width`；已点亮省份的边界在 `.province-line.lit`（要更深才看得出）；
- 高亮省：`.pa-fill` 的 `fill`、`.pa-line` 的 `stroke` 与 `stroke-width`；
- 呼吸光晕颜色与强度：`@keyframes provinceBreathe` 里的 `drop-shadow`（现为淡红 `rgba(226,116,98)`，1px/.30 → 5px/.60），`scale` 是缩放幅度。

**怎么加城市（最省事）**

打开 `js/data.js`，找到 `cities` 部分，里面是一串城市名：

```js
cities: [
  "北京", "天津", "长春", "沈阳", "哈尔滨",
  "大连", "济南", "淄博", "泰安", "青州",
  "徐州", "连云港", "成都", "重庆", "武汉", "大同"
],
```

每行一个城市，**有两种写法**：

1. **只写城市名（自动识别省份）**：`"西安"` → 自动识别为陕西并点亮陕西。系统内置了「城市→省份」对照表（约 150 个主要城市）。
2. **显式指定省份（推荐，尤其小众地名）**：写成 `"城市名@省份"`，用 `@` 隔开。例如 `"凤凰@湖南"`、`"漠河@黑龙江"`。任何对照表里没有的小众地名，用这种写法最稳，直接点亮你指定的省。

- **想新增**：复制一行，注意逗号隔开。
- **想删除**：删掉对应那一行即可。
- **直辖市 / 特别行政区**：北京、天津、上海、重庆、香港、澳门，直接写城市名即可，弹窗里的小字会自动显示「中国」而不是重复城市名。
- 「抵达城市」的数字会自动等于城市数量，无需手动改。

**只能后台编辑，访客不可点**

地图上的省份是**纯展示**，访客无法点击点亮。点亮的省份完全由你在这里填写的城市清单决定——加一个城市，对应省份自动点亮；删掉城市，对应省份自动熄灭。这是唯一的控制方式，简洁又不会被人乱点。

> 提示：如果某座城市不在内置对照表里，弹窗顶部会提示「未识别的城市」，此时把它写进 `cities` 清单前，可先在对照表里补一条，或换个更常见的城市写法即可。

---

## 三、怎么上传照片（按城市分组的图集）

照片是按**城市图集**组织的：同一个城市的所有照片归为一本图集，网站上显示一张封面（该城市第一张照片），右下角标有几张，鼠标悬浮时会浮现一枚朱红印章。点开后可以在这个城市的照片里左右翻看。

1. 把您的照片复制进 `assets/images` 文件夹（建议 jpg / png / webp 格式）；
2. 打开 `js/data.js`，找到 `photos` 部分，每个城市是这样一段：

```js
{
  city: "中国 重庆",           // 城市名，显示在封面下方
  seal: "渝",                 // （可省略）这个城市专属的悬浮印章文字，不写就用统一的「影」
  star: true,                 // （可省略）星标，写了这本图集左上角会多一枚铜钱标记，详见下方
  photos: [
    { src: "assets/images/travel-16.jpg", title: "嘉陵江意" },
    { src: "assets/images/travel-17.jpg", title: "长江国际" }
  ]
},
```

3. 给已有城市加照片：往它的 `photos` 里加一行 `{ src: "...", title: "..." }` 即可；
4. 加新城市：复制上面整段，改 `city` 和里面的照片，注意与前一段之间用逗号隔开；
5. 保存并刷新网页。

**改悬浮印章那个字**：

- 想让所有图集统一换字，改 `data.js` 顶部 `site` 里的 `photoSeal`：
  ```js
  photoSeal: "影",
  ```
- 只想让某一个城市用别的字，就在那个城市里写一行 `seal: "渝",`（不写就自动用统一的字）。
- 建议 1 个字最好看；写 2 ~ 3 个字也可以，印章会自动缩小变成长条。

**给图集加星标（铜钱标记）**

想突出某本图集（比如最满意的一座城市），可以在那个城市里写一行：

```js
star: true,
```

写入后，这本图集的**左上角**会骑着一枚**圆形铜钱**标记：深朱色、实心、中间是带圆角的方孔，会缓缓呼吸缩放，鼠标移到卡片上时会略微变亮并与卡片一起上浮。

- **取消星标**：删掉这行，或改成 `star: false`；
- 它可以和 `seal` 同时存在，一个在角上一个在悬浮时浮现，互不冲突；
- 只有你自己能在 `data.js` 里设置，访客在网页上看不到任何开关。

想调整这枚标记的外观，改 `css/style.css` 里 `.album-star` 那一段：

| 想改什么 | 改哪一项 | 现在的值 |
| --- | --- | --- |
| 大小 | `width` / `height` | 29px |
| 位置（数值越小越往卡片里挪，负数表示探出卡片外） | `top` / `left` | -9px |
| 浓淡（0 ~ 1，越小越淡） | `opacity` | 0.8 |
| 颜色 | `.coin-body` 的 `fill` | 深朱 `#8C2F28` |
| 呼吸快慢 / 幅度 | `animation` 的时长、`coinBreathe` 里的 `scale` | 4.6s / 1.14 |
| 方孔里的底色（应与卡片纸色一致） | `.coin-hole` 的 `fill` | `var(--card)` |

> 小提示：铜钱画在 `js/main.js` 的 `starHTML()` 里，是一段 SVG。方孔的大小和圆角在 `<path class="coin-body">` 的第二个子路径里改（`M16.6,13.3 ... A3.3,3.3` 中的 3.3 就是圆角半径），底下的 `<rect class="coin-hole">` 要跟着把 `rx` 调大一点点，保证底色能盖满方孔。

**换头像/肖像**：把您的竖版照片（建议 3:4 比例）放进 `assets/images`，然后在 `data.js` 的 `profile.portrait` 一行把路径改为：

```js
portrait: "assets/images/我的肖像.jpg",
```

**给旅行随笔换配图**：同样把照片放进 `assets/images`，然后改 `travel` 里对应文章的 `cover` 路径。

> 目前网站里展示的是水墨风格的占位图，替换后自动消失。若某个图片路径写错了，网页也会自动显示占位图提醒您。

---

## 四、影像集：改成社交平台入口

「影像集」页签不放本地视频，而是放您在各平台的入口卡片，点一下就跳转到您的主页。

在 `data.js` 的 `social` 部分修改：

```js
{
  name: "抖音",                        // 平台名
  desc: "旅行碎片与随手拍",              // 一句话说明
  link: "https://v.douyin.com/xxxxx/"  // 您的主页链接
},
```

新增平台就复制一段改内容；想去掉就整段删掉。

---

## 五、一键上传到 GitHub（push.bat）

项目里有一个 **push.bat**，双击就能把网站传上去，不用记任何命令。

**第一次使用（只需做一次）**

1. 打开你现有的仓库页面（或新建一个），复制仓库地址，形如 `https://github.com/你的用户名/仓库名.git`
   - 查找方法：仓库页面 → 绿色 `Code` 按钮 → 选 HTTPS → 复制
   - 如果你只知道访问网址 `https://用户名.github.io/仓库名/`，那么仓库地址就是 `https://github.com/用户名/仓库名.git`
   - 想换新仓库时才需要新建，沿用旧仓库直接填它的地址即可；
3. 双击 `push.bat`，按提示依次填入：你的名字、邮箱、仓库地址；
4. 弹出登录窗口时用 GitHub 账号登录（密码处用 Personal Access Token，详见下方）；
5. 看到 `Uploaded successfully` 就成功了。

**以后每次更新**

双击 `push.bat`，输入一句更新说明（直接回车则用默认的 `site update`），等它跑完即可。仓库地址和身份都已配置好，不会再问你。

> 当前配置：地址 `https://github.com/Sifreelee/self_introduction.git`，分支 `main`，网址 <https://sifreelee.github.io/self_introduction/>。想改地址就在文件夹里打开命令行执行 `git remote set-url origin 新地址`。

**几个注意点**

- 上传前先跑一次 `compress.bat`（压缩 + 统一转 jpg），脚本会自动拦截超过 25MB 的文件并提示你，避免传一半失败；
- 沿用已有仓库时，脚本会检测到里面已有旧文件，并让你选怎么处理（**网址始终不变**）：
  - 输入 `1`（直接回车也是它）：以你本地的版本为准合并，旧文件保留在仓库里
  - 输入 `2`：用本地内容整个替换，GitHub 上的旧文件会被删除
  - 输入 `3`：取消，什么都不传
- 选 1 合并后，仓库里会同时留下新旧文件（例如旧的 `travel-4.png`、几个 `travel-*.svg` 占位图、`README.md`）。网页不会引用它们，可以进仓库网页删掉省空间；
- 万一合并或替换失败，脚本会安全回滚并说明原因，你的本地文件不会丢；
- 想让网站能被访问：进仓库 → Settings → Pages → Branch 选 `main`、目录选 `/ (root)` → Save，稍等一两分钟就会给出网址；
- 之后每次 push 完，网站会在一两分钟内自动更新。

**关于密码**：GitHub 早已不支持用账号密码登录 git。弹窗登录时选浏览器登录最省事；若要求填密码，需要去 GitHub → Settings → Developer settings → Personal access tokens 生成一个 token（勾选 `repo`），粘贴到密码框。

---

## 六、常见问题

**Q：改了 data.js 但网页没变化？**
强制刷新一下：按 `Ctrl + F5`（Mac 按 `Cmd + Shift + R`）。

**Q：改了 style.css / main.js，强制刷新还是老样子？**
`index.html` 里样式表和脚本后面都带了版本号：

```html
<link rel="stylesheet" href="css/style.css?v=20260916c">
<script src="js/data.js?v=20260916c"></script>
<script src="js/china-map.js?v=20260916c"></script>
<script src="js/main.js?v=20260916c"></script>
```

每次改完这些文件，把末尾这个版本号改一下（比如 `...b` 改成 `...c`），浏览器就会重新下载，不用再手动清缓存。

**Q：本地改好了，但线上（GitHub Pages）还是旧的？**
本地文件不会自动同步到网上，双击 `push.bat` 上传一次才会更新（详见第五节）。

**Q：照片显示不出来？**
检查文件名是否完全一致（包括大小写和扩展名），路径应写成 `assets/images/文件名`。

**Q：想改颜色 / 字体？**
配色定义在 `css/style.css` 文件最顶部的 `:root` 区块（如朱砂红 `--cinnabar`、黛青 `--teal`、宣纸底 `--paper`），改色值即可。

**Q：随笔正文想换行分段？**
`content` 里每一段放在一对引号里，用逗号隔开，就是一段。

**Q：想加更多爱好图标？**
`hobbies` 的 `icon` 目前支持：brush（毛笔）、camera（相机）、tea（茶）、mountain（登山）、book（书）、guqin（琴）、chess（棋）、flower（花）、music（乐）、run（跑）、stamp（邮票）。

**Q：照片集 / 旅行随笔一页显示多少个？**
在 `js/main.js` 里找这一行：

```js
const PAGE_SIZE = { travel: 3, photos: 9 };
```

`travel` 是随笔每页篇数，`photos` 是图集每页本数，改数字保存即可（图集建议用 3 的倍数，网格才整齐）。

**Q：照片集上面的搜索框能搜什么？**

相册名和照片名都行，边打边筛：

- 输「北京」→ 名字含北京的**相册**；
- 输「八大局」→ 标题含这几个字的**单张照片**；
- 输「zb201」→ 按**文件名**也能命中（不用管路径和 .jpg）；
- 命中的字会标成朱红；结果**不分页、一次全列**；把框里清空就回到原来的分页界面。

点相册卡片照旧是先弹缩略图选择器；点搜出来的单张照片则**直接放大**，左右键在这个图集里前后翻。

---

## 文件结构一览

```
personal-website/
├── index.html            ← 网页入口（双击打开）
├── 使用说明.md            ← 本文件
├── add-tool.html         ← 内容添加小工具（填表生成代码）
├── compress.bat          ← 一键压缩并统一转 jpg（双击）
├── compress.py           ← 压缩脚本，给 compress.bat 用的
├── push.bat              ← 一键上传 GitHub（双击）
├── css/
│   └── style.css         ← 样式（配色在顶部 :root）
├── js/
│   ├── data.js           ← ★ 您的内容都在这里（含 cities 城市清单）
│   ├── china-map.js      ← 中国地图省级轮廓数据（无需改动）
│   └── main.js           ← 渲染引擎（无需改动）
└── assets/
    └── images/           ← 照片放这里
        └── *.svg         （水墨占位图，某张图路径写错时会自动顶上）
```

日常只需要记住三件事：照片丢进 `assets/images`、文字改 `js/data.js`、做完双击 `push.bat` 上传。
