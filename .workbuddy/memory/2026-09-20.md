# 2026-09-20 工作记录

## 照片集搜索框（行迹 · 照片集）
- 新增 `.album-search` 纸色搜索框（`index.html` 的 `#panel-photos` 内，位于 `.photo-grid` 之上），配色沿用 `--paper-2` / `--line`，聚焦转朱红，非纯白。
- `js/main.js`：`photoQuery` + `initAlbumSearch()` + `setSearchHint()`；`renderPhotos()` 搜索时跳过分页一次列全，清空恢复分页。
- 搜索框间距：`.tabs` 自带 48px 下边距，搜索框默认 0 上边距会显得紧贴相册 → 用 `margin: -16px auto 32px` 把它上提并拉开与相册的距离；`.album-search-hint` 用 `margin: -18px auto 26px` 抵消，保证提示紧贴框下方。

## 搜索扩展到照片名（本次）
- 命中分两类：相册名（`g.city`）→ 相册卡片；照片名 → 单张照片卡片（`.photo-shot`，带 `data-gi` / `data-pi`）。
- 照片名匹配 = `p.title` + 文件名裸名 `shotBaseName(p.src)`（`assets/images/zb2/zb201.jpg` → `zb201`），所以标题没写也能按文件名搜到。
- 点照片结果直接进灯箱（`albumPhotos = g.photos` 后 `openLightbox(pi)`），左右键在该图集内前后翻；点相册卡片仍是先弹缩略图选择器。
- `hitHTML()` 把命中片段包成 `<b class="hit">`（朱红）。坑：`raw.toLowerCase().indexOf(q)` 在 q 含大写时永远 -1，必须 `indexOf(q.toLowerCase())`。
- 测试办法：正则从 main.js 抽出纯函数 `shotBaseName` / `hitHTML` 在 Node 里 eval 后断言（14 项全通过）。agent-browser 在本机会卡死（7 分钟无输出），不要再依赖它做验证。

## add-tool 清单解析：支持文件名带空格
- 老逻辑按「第一个空格」切分 → `圆明园 正觉寺后门.jpg 圆明园 正觉寺后门` 被切成 src=`圆明园.jpg`、title=`正觉寺后门.jpg 圆明园 正觉寺后门`，全错。
- 新逻辑 `splitLine()`：先用 `/\.(jpe?g|png|webp|gif|svg|avif|bmp)(?=\s|$)/gi` 找**最后一个**「扩展名 + 行尾/空白」的位置下刀；没写扩展名才退回制表符 / 第一个空格；`tidy()` 负责去掉首尾的中英文空白（含全角空格 U+3000、NBSP U+00A0）。
- 行切分改用 `split(/\r?\n/)` + tidy + filter(Boolean)（filter(Boolean) 不能直接吃 map 的结果？可以，tidy 返回字符串，注意 `filter(Boolean)` 是合法的）。
- 复用抽函数的办法做验证：抽出 tidy/splitLine/fixExt/normPath/esc + IMG_EXT/EXT_SPLIT_RE 常量，11 项断言全过，内联脚本 `node --check` 通过。
- 顺手修 `js/data.js` 里 3 条被老逻辑污染的标题（205-207 行）：`正觉寺后门.jpg `→`正觉寺后门`、`绮春园 .jpg `→`绮春园`、`绮春园 其二.jpg 圆明园 绮春园5`→`绮春园 其二`。复核：标题残留扩展名 0 条、反斜杠路径 0 条、280 个 src 文件全部存在。
