# 2026-09-16

## 伪满建筑图集：图片批量重命名
- 将 assets/images 下 2026-09-16 15:34 新导入的 32 张伪满建筑照片按导入时间顺序重命名为 weiman1~weiman32（原中文名：东本愿寺 → 鸣放宫摩天轮同框）。
- 脚本：`rename_weiman.py`（项目根目录），按 mtime 过滤批次 + 临时名中转重命名，避免撞名；可重复运行。
- 产出：`assets/伪满图集_编号对照表.csv`（UTF-8 BOM）、`assets/伪满图集_照片清单.txt`（add-tool.html 照片清单框格式：文件名 + 空格 + 标题）。
- 原图备份已移出工作区：`F:\WorkBuddy\_backup_weiman_2026-09-16\`（原中文名，验证无误后可删）。
- 环境备注：本工作区 Bash 工具不可用（PortableGit 缺 coreutils），改用 PowerShell；PowerShell 工具不回显 stdout，需写临时文件再 Read 读取（控制台中文会因编码显示为乱码，读文件时正常）。

## compress.py 改为不保留原图（用户要求）
- 去掉 BACKUP_DIR 与 `F:\WorkBuddy\_原图备份_个人网站` 备份逻辑，压缩后直接原地覆盖。
- 安全兜底：`save_jpg()` 先写 `__tmp_<pid>.jpg`，成功后 `os.replace()` 覆盖；失败则删临时文件、保留原图。
- `save_jpg()` 改用 `with Image.open()`，确保替换/删除原图前释放文件句柄（Windows 否则报占用）。
- png→jpg 且目标同名 jpg 已被另一张图占用时：跳过并提示，不覆盖也不删除。
- 同步更新 `使用说明.md` 第 5 条与 `compress.bat` 提示文案。
- 沙盒测试脚本思路：`sys.modules["ctypes"]=None` 屏蔽 MessageBox 弹窗，用 importlib 加载 compress.py 后改写 IMG_DIR/DATA_JS 到临时目录验证（8 项断言全通过）。
